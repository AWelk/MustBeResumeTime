export class Institution {

  constructor(public institution: string, public city: string, public state: string, public degree: string,
              public startDate: string, public endDate: string, public achievements: string) {
  }
}
